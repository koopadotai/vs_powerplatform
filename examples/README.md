# Example Applications

Reference implementations demonstrating end-to-end patterns.

| Example | What it shows |
|---|---|
| `asset-management/` | Canvas App + Dataverse + .NET API + auth + ALM. The canonical sample. |

Each example has:
- Source code (`.pa.yaml`, `.cs`, etc.)
- Schema documentation
- Deployment instructions
- Architecture diagrams

> Phase 1 ships with the directory. The asset-management reference app is built in Phase 2.
