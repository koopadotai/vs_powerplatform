# Calculator POC — Power Apps Canvas

A minimal proof-of-concept Canvas App. **No data sources, no Dataverse, no schema** — just two screens that prove the toolkit's PAC CLI → MCP build pipeline works end-to-end.

## What it does

### MainScreen — the calculator
- 4-function arithmetic: `+`, `−`, `×`, `÷`
- Decimal point support
- Clear (`C`) button
- Equals (`=`) button
- Chained calculations (e.g. `2 + 3 + 4 =` → `9`)
- Tap **"© Copyright by WeeSiong"** at the bottom to navigate to the Copyright screen

### CopyrightScreen
- User avatar (first letter of `User().FullName`)
- "Welcome back, **`User().FullName`**"
- "You are using: **Calculator POC**" (the AppName named formula)
- "© Copyright by WeeSiong" + "© 2026 · All rights reserved"
- Back button

## Files

```
calculator-poc/
├── canvas/
│   ├── App.pa.yaml             # Theme + AppName + AppVersion + initial state
│   ├── MainScreen.pa.yaml      # The calculator
│   └── CopyrightScreen.pa.yaml # Welcome + copyright
├── CalculatorPOC.msapp         # Generated by Pack-Canvas.ps1 (gitignored)
└── README.md
```

## State variables

The calculator is pure in-memory state — no data source needed.

| Variable | Type | Purpose |
|---|---|---|
| `varDisplay` | Text | What's currently shown on the display |
| `varStoredValue` | Number | The accumulator (left side of pending operation) |
| `varOperator` | Text | Pending operator — `"+"`, `"-"`, `"*"`, `"/"`, or `""` |
| `varJustCalculated` | Boolean | True right after `=` or operator press; clears on next digit |

## Deploy in ONE command (recommended)

If you've already run `Install-All.ps1`, the entire deploy is **3 human actions + 1 Claude prompt**:

### Step 1 — Create an empty Canvas App in Studio (browser)

1. Open https://make.powerapps.com
2. Click **+ New app → Canvas → Phone**
3. Save it (name doesn't matter — the toolkit will overwrite the screens)
4. Click ⚙ **Settings → Updates → Coauthoring** → toggle **ON**

### Step 2 — Copy the Studio URL

It's the URL in your browser tab right now. It contains `appid=...` and `environment-id=...`.

### Step 3 — Ask Claude to deploy

In Claude Code, paste:

```
Deploy calculator-poc to <your-studio-url>
```

That's it. Claude will:
- Verify the canvas-apps plugin is available
- Configure canvas-authoring MCP for your Studio URL
- Read `examples/calculator-poc/canvas/` (App, MainScreen, CopyrightScreen)
- Push the YAML to Studio via `mcp__canvas-authoring__compile_canvas`
- Verify and report back

Refresh Studio in your browser — the calculator is running.

> Don't have Claude Code? Run `.\scripts\windows\Install-All.ps1` first — it installs the Claude Code CLI and the canvas-apps plugin needed by `/deploy-example`.

---

## Manual deploy (if you can't use MCP)

### Why PAC CLI `pack` doesn't work on hand-authored YAML

`pac canvas pack` only works on YAML that was originally **unpacked from a real `.msapp`** (round-trip). It cannot build a `.msapp` from YAML written from scratch — Microsoft documents this limitation:

> "Canvas apps packed using yaml SourceCode must be validated first by opening the app for edit within the Power Apps studio."

For toolkit-authored YAML, the canvas-authoring MCP server is the only direct deploy path.

### Round-trip workflow (existing apps)

If you already have a `.msapp` and want to edit its YAML locally:

```powershell
# Unpack
pac canvas unpack --msapp .\my-app.msapp --sources .\my-app-src --layout SourceCode

# Edit YAML files in .\my-app-src\

# Repack
.\automation\Pack-Canvas.ps1 -Source .\my-app-src -Output .\my-app.msapp -Force
```

## Standards followed

- ✅ All controls follow naming convention (`btn`, `lbl`, `rect`)
- ✅ All colors via named formulas (no hardcoded RGBA in screens)
- ✅ `AppVersion` label visible on both screens
- ✅ `AppName` defined as named formula (used in the welcome message)
- ✅ Math errors wrapped in `IfError` with `Notify`
- ✅ No data source dependency (offline-capable)

## Why this is a good POC

It exercises the **complete toolkit pipeline** without any data complexity:

```
Hand-authored YAML
       │
       ▼
PAC CLI (pac canvas pack)
       │
       ▼
.msapp package
       │
       ▼
Power Apps Studio (manual import OR MCP compile_canvas)
       │
       ▼
Running app in browser/mobile
```

If this works, every standard, naming, theming, and build mechanic is correctly wired. After this, building a real app (with Dataverse, APIs, etc.) is just adding scope on top of a verified foundation.

## Customizing

| To change... | Edit... |
|---|---|
| App display name | `App.pa.yaml` → `AppName = "..."` |
| Theme colors | `App.pa.yaml` → color formulas |
| Button layout | `MainScreen.pa.yaml` button positions |
| Calculator scope (e.g. add `%`, memory keys) | `MainScreen.pa.yaml` — add buttons + state |
| Copyright text | `CopyrightScreen.pa.yaml` `lblCopyrightLine.Text` |
